__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
outputs/*.mcap

# Raw nuScenes dataset (4 GB) — downloaded via scripts/download_nuscenes.py, never committed
data/nuscenes/

# Extracted real tracks (small .npz) ARE committed so the filter runs without the dataset.
